# Service mesh decision

No mesh is installed. See [the evaluation](../docs/service-mesh.md). Any mesh
must demonstrate acceptable measured memory per Pi and explicit control-plane
outage and identity-recovery behavior.
